# SQLBot 智能问数功能完整调用链路

## 前端调用链路

### 1. 前端发起智能问数请求
- **入口**: `frontend/src/views/chat/index.vue` - 用户在聊天界面输入问题并提交
- **API调用**: `fronte4d/src/api/chat.ts` 中的 `questionApi.add(data, controller)` 方法
- **实际请求**: `request.fetchStream('/chat/question', data, controller)` - 使用流式请求发送问题
- **数据结构**: 使用 `ChatQuestion` 模型传递聊天ID和问题内容

## 后端调用链路

### 2. 后端API接收请求
- **API入口**: `backend/apps/chat/api/chat.py` 的 `@router.post("/question")` 函数
- **函数名**: `stream_sql(session: SessionDep, current_user: CurrentUser, request_question: ChatQuestion, current_assistant: CurrentAssistant)`
- **功能**: 接收前端的智能问数请求，返回流式响应

### 3. LLM服务初始化
- **服务类**: `backend/apps/chat/task/llm.py` 中的 `LLMService` 类
- **创建实例**: `LLMService.create(session, current_user, request_question, current_assistant, embedding=True)`
- **初始化**: 创建LLM配置、聊天模型实例和消息历史

### 4. 记录初始化
- **方法**: `llm_service.init_record(session=session)` - 初始化聊天记录
- **数据存储**: 在数据库中创建新的聊天记录，状态为进行中

### 5. 异步任务执行
- **方法**: `llm_service.run_task_async()` - 启动异步任务
- **实际执行**: `executor.submit(self.run_task_cache, in_chat, stream, finish_step)` - 提交到线程池执行

### 6. 核心处理流程 (`run_task` 方法)
在 `backend/apps/chat/task/llm.py` 的 `run_task` 方法中执行以下步骤：

#### 6.1 数据源验证
- **数据源检查**: 验证聊天记录中的数据源是否存在
- **动态数据源**: 如果是动态数据源类型，获取外部数据源配置
- **表结构获取**: 通过 `get_table_schema` 获取数据源的表结构信息
- **专业术语**: 通过 `get_terminology_template` 获取相关专业术语
- **数据训练**: 通过 `get_training_template` 获取数据训练模板

#### 6.2 连接验证
- **连接检查**: `check_connection(ds=self.ds, trans=None)` - 验证数据源连接
- **异常处理**: 如果连接失败抛出 `SQLBotDBConnectionError`

#### 6.3 SQL生成
- **方法**: `generate_sql(_session)` - 使用大语言模型生成SQL语句
- **消息构建**: 构建系统提示和用户问题的消息序列
- **流式处理**: `process_stream(self.llm.stream(self.sql_message), token_usage)` - 流式获取模型响应
- **结果保存**: `save_sql_answer` - 保存生成的SQL到数据库

#### 6.4 SQL执行与数据查询
- **SQL解析**: 从生成的响应中提取SQL语句
- **数据库执行**: `exec_sql(ds: CoreDatasource | AssistantOutDsSchema, sql: str, origin_column=false)` - 执行SQL查询
- **多数据库支持**: 支持MySQL、PostgreSQL、SQL Server、Oracle、ClickHouse等多种数据库
- **数据处理**: 将查询结果转换为统一的数据格式
- **结果保存**: `save_sql_exec_data` - 保存查询结果到数据库

#### 6.5 图表生成
- **方法**: `generate_chart(_session, chart_type: Optional[str] = '')` - 生成图表配置
- **图表类型判断**: `get_chart_type_from_sql_answer(full_sql_text)` - 根据数据判断合适的图表类型
- **图表配置**: 生成图表的JSON配置信息
- **结果保存**: `save_chart` - 保存图表配置到数据库

#### 6.6 数据库操作
- **数据访问层**: `backend/apps/db/db.py` - 多数据库支持的统一访问接口
- **连接管理**: `get_engine(ds: CoreDatasource, timeout: int = 0)` - 根据数据源类型创建数据库引擎
- **SQL执行**: `exec_sql` - 根据不同的数据库类型执行SQL并格式化返回结果
- **支持的数据库类型**:
  - MySQL: 使用 `pymysql`
  - PostgreSQL: 使用 `psycopg2`
  - SQL Server: 使用 `pymssql` 
  - Oracle: 使用 `oracledb`
  - ClickHouse: 使用 `clickhouse-sqlalchemy`
  - 达梦数据库: 使用 `dmPython`
  - Redshift: 使用 `redshift_connector`
  - Elasticsearch: HTTP API

### 7. 流式响应返回
- **响应类型**: `StreamingResponse(llm_service.await_result(), media_type="text/event-stream")`
- **数据格式**: 使用 Server-Sent Events (SSE) 格式返回数据
- **消息类型**:
  - `{type: 'id', id: record_id}` - 返回记录ID
  - `{type: 'sql-result', content: ..., reasoning_content: ...}` - SQL生成过程
  - `{type: 'info', msg: 'sql generated'}` - SQL生成完成通知
  - `{type: 'chart-result', content: ..., reasoning_content: ...}` - 图表生成过程
  - `{type: 'chart', content: chart_config}` - 图表配置
  - `{type: 'finish'}` - 处理完成通知

## 数据模型

### 核心数据模型
- **ChatQuestion**: 智能问数请求模型，包含聊天ID和问题内容
- **ChatRecord**: 聊天记录模型，存储问题、SQL、查询结果、图表等信息
- **CoreDatasource**: 数据源模型，包含数据库连接配置信息
- **LLMConfig**: 大语言模型配置模型

## 关键功能组件

### AI模型管理
- **位置**: `backend/apps/ai_model/`
- **功能**: 模型配置、工厂模式创建、模型选择

### 数据源管理
- **位置**: `backend/apps/datasource/`
- **功能**: 数据源配置、连接验证、表结构获取

### 嵌入与RAG
- **位置**: `backend/apps/terminology/`, `backend/apps/data_training/`
- **功能**: 专业术语检索、数据训练模板、语义相似度匹配

### 图表服务端渲染
- **位置**: `g2-ssr/`
- **功能**: 图表的服务器端渲染与图片生成

## 总结

SQLBot 智能问数功能的完整链路是从前端用户界面输入自然语言问题开始，通过流式API传输到后端，利用大语言模型进行SQL生成，执行数据库查询，最后生成可视化图表并返回给前端展示。整个过程涉及前端界面、API接口、LLM服务、数据库操作和图表渲染等多个组件的协同工作。